---
title: "prac2"
editor: visual
---

```{r}
set.seed(1)
  n<-100
x<-seq(1:n)
x
ei<-rnorm(length(x),mean=0,sd=0.2)
y<- sin(x/10)+ei
y
df7<-data.frame(x,y)

customLowess<-function(x,y,f){
  k=ceiling(f*n)
  smooth_values<-numeric(n)
  for (i in seq_along(x)) {
    # calculate weights
    distLowess<- abs(x-x[i])
    neighbors<-order(distLowess)[1:k]
    Dmax<-max(distLowess[neighbors])
    neighbor_weight <- (1 - (distLowess[neighbors] / Dmax)^3)^3
     # fitting regression
    #neighbor_weight[distLowess[neighbors] >= Dmax] <- 0  
    #weighted regression 
    X7<- cbind(1,x[neighbors] )
    X7
    W7<-diag(neighbor_weight)
    W7
    Y<-y[neighbors]
    bhat7<-solve(t(X7)%*%W7%*%X7) %*% (t(X7)%*%W7%*%Y)
    bhat7
    smooth_values[i] <- bhat7[1] + bhat7[2] * x[i]
  }
  smooth_values
}

plot(df7$x, df7$y)

lines(customLowess(df7$x,df7$y,0.3), col='brown')

  
  


```

Q2

```{r}
df7<-data.frame(x,y)
df7
plot(df7$x,df7$y) #Create scatterplot
lines(lowess(df7$x,df7$y,f=0.3), col='red')


```
